<instructions>
  <mission>Fix quality gate failures for Epic {epic_num} Story {story_num}</mission>

  <context>
    Quality gate checks have failed. A failure report is attached below with
    the exact commands that failed, their exit codes, and full output.
    The story file is included in context — use it to understand what was
    implemented, which files were changed, and what the acceptance criteria are.
    Your job is to apply minimal, targeted fixes so the quality gates pass.
  </context>

  <steps>
    <step number="1" name="Understand Context">
      Read the story file to understand:
      - What tasks were implemented and which files were changed
      - The acceptance criteria and testing requirements
      - The Quality Gates table showing which checks are expected

      Then read the qa-failure-report section below to understand exactly
      what failed and why.
    </step>

    <step number="2" name="Analyze Failures">
      For each failure in the report:
      - Read the failing files to understand the actual code
      - Identify the root cause (not just the symptom)
      - Check whether failures are related (cascading errors)

      ROOT CAUSE GROUPING — before fixing anything, count unique root causes:
      - If many test failures share the same stack trace or error message, they
        have ONE root cause. Fix it once — do not analyze each failure separately.
      - The failure report groups identical failures automatically. Look for
        "Root Cause N (X occurrences)" sections and treat each group as a single fix.
      - If all N failures originate from the same function call site (same file:line),
        the fix is at that call site or in the test setup that fails to mock it.

      PRIORITY ORDER — fix in this order since earlier fixes often resolve later ones:
      1. Build errors (missing imports, syntax errors)
      2. Type errors (wrong types, missing annotations)
      3. Lint errors (style violations, unused imports)
      4. Test failures (broken assertions, missing mocks)
    </step>

    <step number="3" name="Apply Targeted Fixes">
      Fix ONLY what is broken. For each failure type:
      - **Type errors**: Add or correct type annotations, fix import paths
      - **Lint errors**: Fix the specific violations reported (unused imports, formatting)
      - **Test failures**: Fix the failing tests or the code they test — check if the test
        expectations match the actual implementation
      - **Build errors**: Resolve missing dependencies, syntax issues

      TEST FAILURE DECISION TREE — when tests fail, determine the right fix strategy:
      1. Failures reference a fake/non-existent path (e.g. FileNotFoundError on
         "C:\fake\project\...") → The test uses a mock path. Do NOT create the
         directory. Instead, mock or patch the function that touches the filesystem.
      2. Only PRE-EXISTING tests fail (not the new test file for this story) →
         The story added a new dependency that existing tests don't account for.
         Fix belongs in test fixtures (add a mock/patch to the existing autouse
         fixture or add @patch decorators), NOT in the source code.
      3. The NEW test file fails → The implementation or the test itself has a bug.
         Read both the source and test to determine which is wrong.
      4. Many tests fail at the same call site → Add a single autouse fixture or
         patch that covers all affected tests. Do not add individual patches to
         each test function.

      CRITICAL CONSTRAINTS:
      - Do NOT refactor unrelated code
      - Do NOT add new features or improvements
      - Do NOT change code that is not directly causing failures
      - Keep changes as small and focused as possible
    </step>

    <step number="4" name="Verify Fixes">
      Run the failing commands to confirm they now pass:
      - Re-run each command that was listed in the failure report
      - If a command still fails, read the new error output and fix it
      - If fixing one issue reveals new issues, fix those too
      - Continue until all quality gate commands pass
    </step>

    <step number="5" name="Summary">
      Provide a brief summary of changes made:
      - Which quality gates were failing and the root cause of each
      - What fixes were applied (file:line references)
      - Confirmation that verify step passed
      - Note: quality gates will re-run automatically after this step
    </step>
  </steps>
</instructions>
