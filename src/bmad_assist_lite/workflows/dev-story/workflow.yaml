name: dev-story
instructions: instructions.xml

input_file_patterns:
  story_file:
    pattern: "**/{epic_num}-{story_num}*.md"
    strategy: FULL_LOAD
  project_context:
    pattern: "**/project_context*.md"
    strategy: FULL_LOAD

variables:
  mission: "Implement Epic {epic_num} Story {story_num}"
